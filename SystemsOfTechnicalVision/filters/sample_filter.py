#!/usr/bin/python3
"""TVS filter sample"""


def sample_filter(image_data, filter_parameters):
    """Sample of image filter"""
    parameter = filter_parameters["test_parameter"]
    print(f"Sample filter start with parameter: {parameter}")
    return image_data
